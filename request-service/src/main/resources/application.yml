server:
  port: ${REQUEST_SERVICE_PORT:9003}

spring:
  application:
    name: request-service
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: ${REQUEST_DB_URL:jdbc:postgresql://localhost:5432/attendance_request}
    username: ${REQUEST_DB_USERNAME:attendance}
    password: ${REQUEST_DB_PASSWORD:attendance}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:local-development-jwt-secret-change-me-32-chars}
  expiration: ${JWT_EXPIRATION:900000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}
  issuer: ${JWT_ISSUER:auth-service}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:${EUREKA_HOST:localhost}}

springdoc:
  api-docs:
    path: /v3/api-docs

logging:
  pattern:
    console: "%clr(%d{HH:mm:ss.SSS}){faint} %highlight(%-5level) %clr([%20.20thread]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %msg%n%wEx"
  level:
    root: INFO
    com.attendance: DEBUG
